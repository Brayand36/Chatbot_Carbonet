# Google AI (Gemini) — optional: without a key the chatbot uses the local knowledge base
GEMINI_API_KEY=

PORT=3000
NODE_ENV=development

# Comma-separated origins for CORS; leave empty in dev to allow same-origin + tooling
ALLOWED_ORIGINS=

LOG_LEVEL=debug
